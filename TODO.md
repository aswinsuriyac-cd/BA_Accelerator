# TODO

- Optimize the critic retry loop to revise only the specific failed stories instead of regenerating the full package every time.
- Add persistent storage for source BRD, graph state snapshots, critic history, and final approved outputs.
- Verify exported `.xlsx` output against the expected workbook layout with a real sample file comparison.
