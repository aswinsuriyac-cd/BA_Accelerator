# TODO

- Persist BA clarification answers as structured story-level inputs so targeted rework can capture and replay them cleanly.
- Add workflow resume/replay APIs so persisted documents, artifacts, reviews, and exports can be browsed and resumed.
- Verify exported `.xlsx` output against the expected workbook layout with a real sample file comparison.
